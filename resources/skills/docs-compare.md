# Docs Compare Workflow

Use this skill when answering a Zen Cart implementation question with both official docs and local code.

## Steps

- search docs first to find the intended convention
- search the local repo second to find runtime truth
- compare any mismatch explicitly instead of silently preferring one source
- cite concrete file paths for code evidence and page URLs for docs evidence
- call out when the needed implementation likely lives outside the indexed paths
